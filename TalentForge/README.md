# TalentForge

Evidence-based student talent platform — React (Vite) frontend + FastAPI backend + SQLite database.

## Quick start
1. Install **Python 3.10+** (tick "Add Python to PATH") and **Node.js 18+**.
2. Unzip this folder.
3. **Windows:** double-click `start.bat`  ·  **macOS/Linux:** run `bash start.sh`

The first run installs dependencies (a few minutes). The browser then opens http://localhost:3000.
The SQLite database (`backend/talentforge.db`) is created and seeded automatically.

## Demo accounts — password `demo1234`
| Role | Email |
|---|---|
| Student | aarav@talentforge.dev |
| Teacher | ramesh@talentforge.dev |
| Recruiter | recruiter@talentforge.dev |
| Admin | admin@talentforge.dev |

Or use the one-click demo buttons in the Sign In dialog. See `backend/README.md` for manual setup and the API.
